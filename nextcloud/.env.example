# Nextcloud All-in-One Environment Variables

# Timezone
TZ=America/New_York

# Optional: Custom Nextcloud data directory
# NEXTCLOUD_DATADIR=/mnt/nextcloud-data

# Optional: Custom ports (if defaults conflict)
# AIO_PORT=8080
# NEXTCLOUD_PORT=443

# Optional: Skip domain validation (not recommended for production)
# SKIP_DOMAIN_VALIDATION=false

# Optional: Disable backup section
# AIO_DISABLE_BACKUP_SECTION=false
