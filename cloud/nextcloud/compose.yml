services:
  nextcloud-aio-mastercontainer:
    image: nextcloud/all-in-one:latest
    init: true
    restart: unless-stopped
    container_name: nextcloud-aio-mastercontainer
    volumes:
      - nextcloud_aio_mastercontainer:/mnt/docker-aio-config
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # USER CONFIG: Local backup path with SELinux relabeling
      - /etc/share/backups/nextcloud:/mnt/backup:z
    environment:
      # Network: Bind to all interfaces (Required for WireGuard visibility)
      - APACHE_PORT=11000
      - APACHE_IP_BINDING=0.0.0.0
      
      # Validation: Bypass domain checks during setup
      - SKIP_DOMAIN_VALIDATION=true
      
      # Storage: Force data to Unraid NFS Share (Persistent)
      - NEXTCLOUD_DATADIR=/mnt/nas-nextcloud
      - NEXTCLOUD_TRUSTED_PROXIES=10.253.1.3/24,192.168.3.1/24

    ports:
      - 8080:8080
    
    # Network Mode: Explicitly Bridge (Must be commented out when using 'networks')
    # network_mode: bridge

    # Service-level network assignment
    networks:
      - nextcloud-aio   
    
    # CoreOS Constraint: Disable SELinux isolation for Socket access
    security_opt:
      - label=disable
    
    # CoreOS Constraint: Run as root for Socket access
    user: "0:0"

    # FORCE DNS TO BYPASS CLOUDFLARE
    extra_hosts:
      - "nextcloud.nyquil-babies.com:10.253.1.3"

# Global network definition
networks:
  nextcloud-aio:
    name: nextcloud-aio
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: "1350"    

volumes:
  nextcloud_aio_mastercontainer:
    name: nextcloud_aio_mastercontainer